# 仓库协作说明

- Python 环境应当使用：`/home/heqi/miniforge3/envs/qat.yolov5`。
- 当前仓库用于调试 QAT（量化感知训练）模型。
- 所有回复统一使用中文。
- 执行训练等长任务时，不使用 `timeout` 截断；默认保活当前终端会话，不主动关闭或中止进程，除非用户明确要求停止。
- 衡量精度有两方面：1）导出的浮点模型与原始浮点模型差距要小即尽可能达到0.391（BN的eps导致的误差），2）qat模型在开启fake quant的情况下精度要大于0.386
- 尽可能尝试多的参数，epoch、lr等都可以调整
- 本轮实验需在2号卡进行